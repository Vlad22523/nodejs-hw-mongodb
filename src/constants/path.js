import path from 'node:path';

export const TEMPLATES_PATH = path.join(process.cwd(), 'src', 'template');
export const TEMP_PATH = path.join(process.cwd(), 'src', 'temp');
export const UPLOAD_PATH = path.join(process.cwd(), 'src', 'upload');
