openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts app
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: This is a documentation of students app
tags:
  - name: Contacts
    description: Operations about users.
  - name: Auth
    description: Auth operations.
servers:
  - url: http://localhost:3000
  - url: https://nodejs-hw-mongodb-f9j2.onrender.com
paths:
  /contacts:
    get:
      $ref: ../docs/paths/contacts/get.yaml
    post:
      $ref: ../docs/paths/contacts/post.yaml
  /contacts/{contactId}:
    get:
      $ref: ../docs/paths/contacts/{id}/get.yaml
    patch:
      $ref: ../docs/paths/contacts/{id}/patch.yaml
    delete:
      $ref: ../docs/paths/contacts/{id}/delete.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
